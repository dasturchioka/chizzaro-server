require('dotenv').config()
const { PrismaClient } = require('@prisma/client')
const { Request, Response } = require('express')

const prisma = new PrismaClient()

/**
 * @param {Request} req
 * @param {Response} res
 */
async function requestToCreateOrder(req, res) {
	try {
	} catch (error) {}
}	
